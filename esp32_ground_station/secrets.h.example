#pragma once

/*
 * Copy this file to secrets.h and set a private WPA2 password before flashing.
 * secrets.h is ignored by Git and must not be committed.
 */
#define CAR_GS_WIFI_SSID "CAR-GS-xxxx"
#define CAR_GS_WIFI_PASSWORD "replace-with-a-private-password"
